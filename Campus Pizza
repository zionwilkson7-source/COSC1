Main Algorithm

Get numPizzas                          // assume 1 to 5
total = 0
  for i = 1 to numPizzas
        Get radius[i]
        Get price [i]
        valuePerInch[i] = ComputeValue(radius[i], price[i])
        total = total + price[1]
        